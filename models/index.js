// Declare variable User and Thought models imported from their files
const User = require('./User');
const Thought = require('./Thought');

module.exports = { User, Thought}
// Export User and Thought Models